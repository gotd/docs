{
  "name": "docs-v-2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "NODE_OPTIONS=--no-experimental-webstorage docusaurus start",
    "build": "NODE_OPTIONS=--no-experimental-webstorage docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "NODE_OPTIONS=--no-experimental-webstorage docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "NODE_OPTIONS=--no-experimental-webstorage docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "gen:reference": "bash tools/docgen/generate.sh"
  },
  "dependencies": {
    "@docusaurus/core": "^3.10.0",
    "@docusaurus/faster": "^3.10.1",
    "@docusaurus/preset-classic": "^3.10.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.10.0",
    "@docusaurus/types": "^3.10.0",
    "@svgr/webpack": "^8.1.0",
    "@types/react": "^18.0.9"
  },
  "engines": {
    "node": ">=22.0"
  },
  "browserslist": {
    "production": [
      "last 1 chrome version",
      "last 1 safari version"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 safari version"
    ]
  }
}
