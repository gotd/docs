---
title: API Reference
sidebar_label: Overview
sidebar_position: 1
---
{{"{"}}/* Generated by tools/docgen — do not edit. Run `npm run gen:reference`. */{{"}"}}

# API Reference

Complete reference for the gotd MTProto schema, generated directly from the
[`github.com/gotd/td/tg`](https://ref.gotd.dev/pkg/github.com/gotd/td/tg.html) source so
signatures, parameters and documented errors always match the version of the library you
import:

- **[{{.Methods}} methods](./methods)** — RPC calls you invoke on `client.API()`.
- **[{{.Types}} types](./types)** — TL classes (boxed types) and their constructors.
- **[{{.Constructors}} constructors](./constructors)** — concrete TL types and their fields.

:::note[Generated reference]
For an always-current view (and Go-specific helpers) see the full Go reference at
[ref.gotd.dev](https://ref.gotd.dev/pkg/github.com/gotd/td/tg.html). New to gotd? Start
with the [Tutorial](../intro.md) and [Calling the API](../basics/calling-the-api.md).
:::

## Calling a method

Every method is invoked on the raw client returned by `client.API()`:

```go
client.Run(ctx, func(ctx context.Context) error {
	api := client.API()

	// help.getNearestDc — no auth required, a good connectivity check.
	dc, err := api.HelpGetNearestDC(ctx)
	if err != nil {
		return err
	}
	_ = dc
	return nil
})
```

## How to read a method page

| Section | Meaning |
| ------- | ------- |
| **Signature** | Exactly what you call on `client.API()`. |
| **Parameters** | Fields of the method's request; `—` in *Required* marks an optional (conditional) field. |
| **Returns** | The Go type returned (besides `error`). |
| **Possible errors** | Documented Telegram errors; handle them with [`tgerr`](../basics/calling-the-api.md#errors-and-flood_wait) or the [FLOOD_WAIT middleware](../helpers/middleware.md). |

## Namespaces

| Namespace | Methods | Description |
| --------- | :-----: | ----------- |
{{range .Namespaces}}| [`{{.Name}}`](./methods/{{.Name}}) | {{len .Methods}} | {{if .Description}}{{.Description}}{{else}}—{{end}} |
{{end}}
