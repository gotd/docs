---
title: "phone.createConferenceCall"
sidebar_label: "createConferenceCall"
slug: /reference/methods/phone/create-conference-call
description: "Create and optionally join a new conference call."
---
{/* Generated by tools/docgen — do not edit. Run `npm run gen:reference`. */}

# `phone.createConferenceCall`

Create and optionally join a new conference call.

```go
func (c *Client) PhoneCreateConferenceCall(ctx context.Context, request *PhoneCreateConferenceCallRequest) (UpdatesClass, error)
```

## Calling this method

```go
client.Run(ctx, func(ctx context.Context) error {
	api := client.API()
	
	res, err := api.PhoneCreateConferenceCall(ctx, &tg.PhoneCreateConferenceCallRequest{
		// see Parameters
	})
	if err != nil {
		return err
	}
	_ = res // UpdatesClass
	return nil
})
```

## Parameters

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `Muted` | `bool` | — | If set, mute our microphone when joining the call (can only be used if join is set). |
| `VideoStopped` | `bool` | — | If set, our video stream is disabled (can only be used if join is set). |
| `Join` | `bool` | — | If set, also join the call, otherwise just create the call link. |
| `RandomID` | `int` | yes | Unique client message ID required to prevent creation of duplicate group calls. |
| `PublicKey` | `bin.Int256` | — | Public key (can only be used if join is set). |
| `Block` | `[]byte` | — | Initial blockchain block (can only be used if join is set). |
| `Params` | [`DataJSON`](/docs/reference/constructors/general/data-j-s-o-n) | — | Parameters from tgcalls (can only be used if join is set). |

## Returns

[`UpdatesClass`](/docs/reference/types/general/updates)

## References

- [Official documentation](https://core.telegram.org/method/phone.createConferenceCall)
- [Generated Go reference](https://ref.gotd.dev/pkg/github.com/gotd/td/tg.html#name-PhoneCreateConferenceCallRequest)
- TL definition: `phone.createConferenceCall#7d0444bb`
