---
title: "messages.sendPaidReaction"
sidebar_label: "sendPaidReaction"
slug: /reference/methods/messages/send-paid-reaction
description: "Sends one or more paid Telegram Star reactions », transferring Telegram Stars » to a channel's balance."
---
{/* Generated by tools/docgen — do not edit. Run `npm run gen:reference`. */}

# `messages.sendPaidReaction`

Sends one or more paid Telegram Star reactions », transferring Telegram Stars » to a channel's balance.

```go
func (c *Client) MessagesSendPaidReaction(ctx context.Context, request *MessagesSendPaidReactionRequest) (UpdatesClass, error)
```

## Calling this method

```go
client.Run(ctx, func(ctx context.Context) error {
	api := client.API()
	
	res, err := api.MessagesSendPaidReaction(ctx, &tg.MessagesSendPaidReactionRequest{
		// see Parameters
	})
	if err != nil {
		return err
	}
	_ = res // UpdatesClass
	return nil
})
```

## Parameters

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `Peer` | [`InputPeerClass`](/docs/reference/types/general/input-peer) | yes | The channel |
| `MsgID` | `int` | yes | The message to react to |
| `Count` | `int` | yes | The number of stars to send (each will increment the reaction counter by one). |
| `RandomID` | `int64` | yes | Unique client message ID required to prevent message resending. Note: this argument must be composed of a 64-bit integer where the lower 32 bits are random, and the higher 32 bits are equal to the current unixtime, i.e. `uint64_t random_id = (time() &lt;&lt; 32) |
| `Private` | [`PaidReactionPrivacyClass`](/docs/reference/types/general/paid-reaction-privacy) | — | Each post with star reactions has a leaderboard with the top senders, but users can opt out of appearing there if they prefer more privacy. Not populating this field will use the default reaction privacy, stored on the server and synced to clients using updatePaidReactionPrivacy (see here for more info). |

## Returns

[`UpdatesClass`](/docs/reference/types/general/updates)

## Possible errors

| Code | Type | Description |
| ---- | ---- | ----------- |
| 400 | `BALANCE_TOO_LOW` | The transaction cannot be completed because the current Telegram Stars balance is too low. |
| 400 | `CHANNEL_INVALID` | The provided channel is invalid. |
| 403 | `CHAT_WRITE_FORBIDDEN` | You can't write in this chat. |
| 400 | `MESSAGE_ID_INVALID` | The provided message id is invalid. |
| 400 | `PEER_ID_INVALID` | The provided peer id is invalid. |
| 400 | `RANDOM_ID_EMPTY` | Random ID empty. |
| 400 | `RANDOM_ID_EXPIRED` | The specified random_id was expired (most likely it didn't follow the required `uint64_t random_id = (time() &lt;&lt; 32) |
| 400 | `REACTIONS_COUNT_INVALID` | The specified number of reactions is invalid. |
| 400 | `SEND_AS_PEER_INVALID` | You can't send messages as the specified peer. |

## References

- [Official documentation](https://core.telegram.org/method/messages.sendPaidReaction)
- [Generated Go reference](https://ref.gotd.dev/pkg/github.com/gotd/td/tg.html#name-MessagesSendPaidReactionRequest)
- TL definition: `messages.sendPaidReaction#58bbcb50`
