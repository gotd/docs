---
title: "updateServiceNotification"
sidebar_label: "updateServiceNotification"
slug: /reference/constructors/general/update-service-notification
description: "A service message for the user. The app must show the message to the user upon receiving this update. In case the popup parameter was passed, the text message must be displayed in a popup alert immediately upon receipt. It is recommended to handle the text as you would an ordinary message in terms of highlighting links, etc. The message must also be stored locally as part of the message history with the user id 777000 (Telegram Notifications)."
---
{/* Generated by tools/docgen — do not edit. Run `npm run gen:reference`. */}

# `updateServiceNotification`

A service message for the user. The app must show the message to the user upon receiving this update. In case the popup parameter was passed, the text message must be displayed in a popup alert immediately upon receipt. It is recommended to handle the text as you would an ordinary message in terms of highlighting links, etc. The message must also be stored locally as part of the message history with the user id 777000 (Telegram Notifications).

```go
type tg.UpdateServiceNotification struct { /* ... */ }
```

Implements [`Update`](/docs/reference/types/general/update).

## Fields

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `Popup` | `bool` | — | If set, the message must be displayed in a popup. |
| `InvertMedia` | `bool` | — | If set, any eventual webpage preview will be shown on top of the message instead of at the bottom. |
| `InboxDate` | `int` | — | When was the notification receivedThe message must also be stored locally as part of the message history with the user id 777000 (Telegram Notifications). |
| `Type` | `string` | yes | String, identical in format and contents to the type field in API errors. Describes type of service message. It is acceptable to ignore repeated messages of the same type within a short period of time (15 minutes). |
| `Message` | `string` | yes | Message text |
| `Media` | [`MessageMediaClass`](/docs/reference/types/general/message-media) | yes | Media content (optional) |
| `Entities` | [`[]MessageEntityClass`](/docs/reference/types/general/message-entity) | yes | Message entities for styled text |

## References

- [Official documentation](https://core.telegram.org/constructor/updateServiceNotification)
- [Generated Go reference](https://ref.gotd.dev/pkg/github.com/gotd/td/tg.html)
- TL definition: `updateServiceNotification#ebe46819`
