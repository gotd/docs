---
title: "chat"
sidebar_label: "chat"
slug: /reference/constructors/general/chat
description: "Info about a group. When updating the local peer database, all fields from the newly received constructor take priority over the old constructor cached locally (including by removing fields that aren't set in the new constructor). See here » for an implementation of the logic to use when updating the local user peer database."
---
{/* Generated by tools/docgen — do not edit. Run `npm run gen:reference`. */}

# `chat`

Info about a group. When updating the local peer database, all fields from the newly received constructor take priority over the old constructor cached locally (including by removing fields that aren't set in the new constructor). See here » for an implementation of the logic to use when updating the local user peer database.

```go
type tg.Chat struct { /* ... */ }
```

Implements [`Chat`](/docs/reference/types/general/chat).

## Fields

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `Creator` | `bool` | — | Whether the current user is the creator of the group |
| `Left` | `bool` | — | Whether the current user has left the group |
| `Deactivated` | `bool` | — | Whether the group was migrated |
| `CallActive` | `bool` | — | Whether a group call is currently active |
| `CallNotEmpty` | `bool` | — | Whether there's anyone in the group call |
| `Noforwards` | `bool` | — | Whether this group is protected, thus does not allow forwarding messages from it |
| `ID` | `int64` | yes | ID of the group, see here » for more info and the available ID range. |
| `Title` | `string` | yes | Title |
| `Photo` | [`ChatPhotoClass`](/docs/reference/types/general/chat-photo) | yes | Chat photo |
| `ParticipantsCount` | `int` | yes | Participant count |
| `Date` | `int` | yes | Date of creation of the group |
| `Version` | `int` | yes | Used in basic groups to reorder updates and make sure that all of them were received. |
| `MigratedTo` | [`InputChannelClass`](/docs/reference/types/general/input-channel) | — | Means this chat was upgraded to a supergroup |
| `AdminRights` | [`ChatAdminRights`](/docs/reference/constructors/general/chat-admin-rights) | — | Admin rights of the user in the group |
| `DefaultBannedRights` | [`ChatBannedRights`](/docs/reference/constructors/general/chat-banned-rights) | — | Default banned rights of all users in the group |

## References

- [Official documentation](https://core.telegram.org/constructor/chat)
- [Generated Go reference](https://ref.gotd.dev/pkg/github.com/gotd/td/tg.html#name-Chat)
- TL definition: `chat#41cbf256`
